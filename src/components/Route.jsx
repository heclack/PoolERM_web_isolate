import React, {useState, useEffect} from 'react';


//Allows for editing of route

function AppointmentEditor(props){
    const name = useState(`${props.customer.name.last}, ${props.customer.name.first});

}